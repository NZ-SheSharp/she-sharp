# She Sharp - Environment Variables Template
# Copy this file to .env.local and fill in the values.
# For Drizzle/database operations, also copy to .env.

# ============================================================
# Authentication
# ============================================================
AUTH_SECRET=***                        # Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=***                    # Same as AUTH_SECRET
AUTH_URL=http://localhost:3000         # Use https://your-domain.com for production
NEXTAUTH_URL=http://localhost:3000    # Same as AUTH_URL

# ============================================================
# Application
# ============================================================
# The origin every link in an outgoing email is built from. Leave it at the
# production value even locally: a localhost value here reaches real inboxes,
# which is how localhost:3000 got into 25 mentor invitations (2026-03-19) and
# six newsletter confirmations (2026-09-01). Unset now defaults to the canonical
# origin in lib/seo/site.ts rather than to localhost. Override it only while
# testing a link-bearing page, and sendEmail() will warn on every message.
BASE_URL=https://www.shesharp.org.nz

# The site-wide kill switch. `true` makes proxy.ts serve a branded 503 for
# everything, /f/* and the feedback form included — it is the freeze used before
# a database cutover. Absent or anything other than `true` means normal service.
# It takes effect only on the next deploy, like every other variable here:
# there is no Vercel Git connection, so ship it with `gh workflow run deploy.yml`.
# See docs/deployment/MAINTENANCE_MODE.md.
MAINTENANCE_MODE=false

# ============================================================
# Database (Neon PostgreSQL)
# ============================================================
# THE ONE THE APPLICATION USES. lib/db/drizzle.ts throws at module import
# without it, so nothing that touches the database starts. Use the POOLED host
# (the one with `-pooler`): the connection options there are tuned for pgbouncer
# in transaction mode, which is also why `prepare: false` is not optional.
# Until 2026-09-10 this appeared only as a commented-out line below, so anyone
# configuring a machine from this file set DATABASE_URL, got a hard crash, and
# had to read the source to find out why.
POSTGRES_URL=postgresql://user:password@host-pooler.aws.neon.tech/neondb?sslmode=require

# Migrations only — drizzle.config.ts is the sole reader, for db:generate and
# db:migrate. It can be the same string as POSTGRES_URL.
# A per-person Neon role CANNOT run migrations: schema changes need the owner
# role, and drizzle-kit will fail with `must be owner of table`. That is
# deliberate. See docs/deployment/CREDENTIAL_INVENTORY.md.
DATABASE_URL=postgresql://user:password@host-pooler.aws.neon.tech/neondb?sslmode=require

# Deleted 2026-09-10 after a census found no reader for any of them:
# DATABASE_URL_UNPOOLED, NEON_PROJECT_ID, and the twelve PG* / POSTGRES_*
# variables the old Vercel-Neon marketplace integration used to inject. They
# were removed from Vercel on 2026-09-06 and lingered in local files afterwards.
# NEON_PROJECT_ID is the one worth naming: it kept the id of the *retired*
# project while the connection string beside it pointed at the current one, and
# reading those two lines together says the live site is about to be deleted.
# It cost an hour to disprove. Do not re-add any of them.
#
# For a direct (unpooled) connection — pg_dump, or reading pg_stat_activity,
# which the pooler cannot show you — drop `-pooler` from the host of the string
# above rather than storing a second copy that can go stale.

# ============================================================
# OAuth Providers
# ============================================================
# GitHub OAuth App (https://github.com/organizations/NZ-SheSharp/settings/applications)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth Client (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_google_client_secret

# ============================================================
# Email Service (Resend - https://resend.com/api-keys)
# ============================================================
RESEND_API_KEY=re_your_resend_api_key
# Overrides the TRANSACTIONAL From only (lib/email/senders.ts owns the rest).
# Must stay on shesharp.org.nz: Resend DKIM-signs with d=shesharp.org.nz, so any
# other domain loses DMARC alignment and is dropped without a bounce once the
# domain reaches p=reject. See docs/deployment/EMAIL_AUTHENTICATION.md.
EMAIL_FROM="She Sharp <noreply@shesharp.org.nz>"

# Signs the one-click unsubscribe tokens in List-Unsubscribe headers (RFC 8058).
# Any long random string; rotating it invalidates unsent links only.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If unset, notification emails simply ship without the unsubscribe header.
EMAIL_UNSUBSCRIBE_SECRET=

# OPTIONAL mailto: alternative offered alongside the one-click URL. Leave EMPTY
# unless someone has confirmed the address is a real, monitored inbox — some
# clients prefer the mailto, so an address that bounces is worse than none.
# The HTTPS URL alone already satisfies RFC 8058 and the Gmail/Yahoo rules.
EMAIL_UNSUBSCRIBE_MAILTO=

# Resend webhook signing secret (Resend dashboard -> Webhooks -> Add Endpoint,
# pointing at https://www.shesharp.org.nz/api/webhooks/resend). Without it,
# bounces and spam complaints are never recorded.
RESEND_WEBHOOK_SECRET=whsec_your_resend_webhook_secret

# Admin address for "new donation received" notification emails (defaults to website@shesharp.org.nz)
DONATION_ADMIN_EMAIL=website@shesharp.org.nz

# ============================================================
# AI Integration
# ============================================================
# OpenAI (chatbot + mentor matching) - https://platform.openai.com/api-keys
# Also drives CV screening, funding scoring, the Slack event extractor and the
# poster plates. This is the only variable on Vercel production marked Sensitive,
# which means `vercel env pull` returns it as an empty string — that is the flag,
# not a missing value.
OPENAI_API_KEY=sk-proj-your_openai_key

# Model override. Defaults to gpt-4o-mini where it is read.
OPENAI_MODEL=gpt-4o-mini

# Vercel AI Gateway, deliberately dormant: routing the chatbot through it needs
# purchased Gateway credits, so CHATBOT_USE_GATEWAY stays unset and the agent
# talks to OpenAI directly. Set the flag to 1 only together with the key.
AI_GATEWAY_API_KEY=
CHATBOT_USE_GATEWAY=

# ============================================================
# Matching and screening thresholds (all optional, all have defaults)
# ============================================================
# `true` runs AI screening on each volunteer application as it arrives.
AUTO_SCREEN_APPLICATIONS=
# Minimum compatibility score before a mentor/mentee pair is suggested (50).
MATCHING_MIN_SCORE=
# Below this score the pair is dropped before the AI call, to save tokens (30).
MATCHING_PRE_FILTER_THRESHOLD=
# How long a mentee stays in the waiting queue, in days (90).
MATCHING_QUEUE_EXPIRY_DAYS=

# ============================================================
# Upstash Redis / Vercel KV — INJECTED, do not hand-edit
# ============================================================
# The chatbot's rate limiter and question analytics, the newsletter drafts, and
# the matching cache all read Redis. lib/chatbot/redis.ts tries three naming
# conventions in order, which is why there appear to be six variables for one
# store: UPSTASH_REDIS_URL/TOKEN first, then UPSTASH_REDIS_REST_URL/TOKEN, then
# KV_REST_API_URL/TOKEN. Only the last pair is actually set in this project.
#
# The Vercel marketplace integration (`upstash-kv-cerise-leaf`) writes these,
# along with KV_URL, KV_REST_API_READ_ONLY_TOKEN and REDIS_URL, which nothing
# reads. Editing any of them by hand desynchronises the integration and the next
# resource sync silently reverts you. Pull them instead:
#   vercel env pull .env.local --environment=production --scope she-sharp1
UPSTASH_REDIS_URL=
UPSTASH_REDIS_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
KV_REST_API_URL=
KV_REST_API_TOKEN=

# ============================================================
# Vercel Blob (user uploads: profile photos and CVs)
# ============================================================
# Read/write token for the She Sharp Blob store. On Vercel this is injected
# automatically once the store is linked to the project, so it only needs
# setting by hand for local development and for the scripts under scripts/.
# Pull it rather than inventing one:
#   vercel env pull .env.local
# It replaced the three CLOUDINARY_* variables on 2026-09-06, when uploads
# moved off a personally-owned Cloudinary account onto the organisation's own
# Blob store. The store id is embedded in the token and the upload routes
# derive the store hostname from it, so a wrong token fails closed rather than
# writing somewhere unexpected.
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_your_store_id_your_secret

# ============================================================
# Slack Notifications
# ============================================================
# Incoming Webhooks for form submission alerts
SLACK_VOLUNTEER_WEBHOOK_URL=https://hooks.slack.com/services/your/volunteer/webhook
SLACK_CONTACT_WEBHOOK_URL=https://hooks.slack.com/services/your/contact/webhook

# Webhook for "new donation received" alerts. Falls back to SLACK_CONTACT_WEBHOOK_URL if unset.
SLACK_DONATION_WEBHOOK_URL=https://hooks.slack.com/services/your/donation/webhook

# Webhook for post-event attendee feedback (POST /api/event-feedback).
# Posts to #event-feedback-notifications. Optional — falls back to
# SLACK_CONTACT_WEBHOOK_URL if unset, so feedback lands somewhere visible
# rather than vanishing.
SLACK_EVENT_FEEDBACK_WEBHOOK_URL=https://hooks.slack.com/services/your/event-feedback/webhook

# Webhook for the weekly mentorship stats digest (posts to #mentorships every Monday 09:00 NZDT).
# Cron path: /api/cron/weekly-mentorship-stats (schedule: Sun 20:00 UTC)
SLACK_MENTORSHIP_STATS_WEBHOOK_URL=https://hooks.slack.com/services/your/mentorship-stats/webhook

# Webhook for the weekly NZ funding-opportunities digest (posts to #funding-opportunities every Monday 10:00 NZDT).
# Cron path: /api/cron/weekly-funding-crawl (schedule: Sun 21:00 UTC)
SLACK_FUNDING_WEBHOOK_URL=https://hooks.slack.com/services/your/funding/webhook

# Bot token for Slack API access (used by the /event Slack bot and legacy
# local extraction script). Scopes required for the bot:
# commands, chat:write, channels:history, channels:read, groups:history, groups:read
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# OPTIONAL user token for the sync-event-from-slack skill. A bot token can only
# read public channels it joined plus private channels it was invited to, and can
# never read a DM between two people. A user token acts as the authorising human
# and therefore also reads their DMs, group DMs and every private channel they
# are in — no invites needed. Local tooling only; never referenced by the app.
# User Token Scopes: channels:history channels:read groups:history groups:read
#                    im:history im:read mpim:history mpim:read
#                    users:read files:read pins:read bookmarks:read search:read
# Generate at https://api.slack.com/apps/<APP_ID>/oauth (User Token Scopes →
# Reinstall to Workspace). Still cannot read DMs between two OTHER people —
# that needs Slack's Discovery API, which is Enterprise Grid only.
SLACK_USER_TOKEN=xoxp-your-slack-user-token

# OPTIONAL, and only for whoever holds the she-sharp-slack-archive checkout.
# `refresh-archive.ts` refuses to guess this path — a wrong one writes Slack
# transcripts into the wrong repository — so setting it here says it once per
# machine instead of once per run. Refreshing that archive is NOT a step of
# syncing an event: leave this unset and a sync is still complete.
SLACK_ARCHIVE_DIR=/path/to/she-sharp-slack-archive

# ============================================================
# Slack Event Bot (inbound /event slash command)
# https://api.slack.com/apps — see docs/development/SLACK_EVENT_BOT_SETUP.md
# ============================================================
# HMAC secret from the Slack app's Basic Information page
SLACK_SIGNING_SECRET=your-slack-signing-secret
# Comma-separated Slack user IDs permitted to trigger the bot
SLACK_ALLOWED_USER_IDS=U01ABC123,U02DEF456
# Fine-grained GitHub PAT with contents:write + pull_requests:write on the repo
GITHUB_BOT_TOKEN=github_pat_xxxxxxxxxxxxxxxx
# Target repository, "owner/repo" format
GITHUB_REPO=NZ-SheSharp/she-sharp
# Base branch for the bot's PRs (usually "main")
GITHUB_REPO_DEFAULT_BRANCH=main

# ============================================================
# Monthly Newsletter (self-hosted list + Resend batch send)
# The subscriber list lives in the `newsletter_subscribers` table, not in a
# Resend segment. EMAIL_UNSUBSCRIBE_SECRET above is REQUIRED to build a send:
# without it no unsubscribe token can be signed and the batch builder refuses.
# STARTING an issue needs nothing from this file: scripts/newsletter/new-issue.ts
# reads the repo and makes no network call.
# See: docs / .claude/skills/monthly-newsletter/SKILL.md
# ============================================================
# Webhook for the "issue approved" Slack post.
# Optional — falls back to SLACK_CONTACT_WEBHOOK_URL if unset.
SLACK_NEWSLETTER_WEBHOOK_URL=https://hooks.slack.com/services/your/newsletter/webhook

# ============================================================
# Mailchimp Marketing API v3 — LOCAL TOOLING ONLY
# Key page: https://us3.admin.mailchimp.com/account/api/
# Format is <32 hex>-<server prefix>. The suffix IS the hostname
# (https://us3.api.mailchimp.com/3.0/) and is parsed off the end, so a key
# pasted without it fails loudly instead of 401ing against another shard.
# Nothing under app/ reads this and nothing may. Read only by lib/mailchimp/
# and scripts/mailchimp/. Give the key an expiry and record it here.
# ============================================================
MAILCHIMP_API_KEY=
# Optional override; only needed for a key with no <dc> suffix (e.g. OAuth).
MAILCHIMP_SERVER_PREFIX=us3
# The `She#` audience. Not a secret: an audience id is the public half of a
# Mailchimp list — it appeared in the campaign-archive URL the site linked to for
# years, and it grants nothing without the API key above.
# Both things this comment used to point at are gone: `MAILCHIMP_CONFIG` and its
# `lib/data/newsletters.ts` were deleted on 2026-08-30 with the "Open full
# archive" button, and the archive now serves from this repository
# (lib/data/newsletter-archive/). Sign-up is /newsletter/subscribe and the list
# lives in this project's own database. Verified against GET /lists at runtime
# rather than trusted.
MAILCHIMP_LIST_ID=31bd05e8eb
# Raw export vault. Gitignored (/private/); archive of record is the private
# she-sharp-slack-archive repo. Read by scripts/mailchimp/vault.ts, and named
# by lib/data/json/mailchimp/manifest.json — a fresh clone has no other way to
# discover where the CSVs live. Point it at either copy, e.g.
# D:/github_repository/she-sharp-slack-archive/mailchimp/2026-08-17
MAILCHIMP_VAULT_DIR=

# ============================================================
# Humanitix Public API — read-only
# Key page: https://console.humanitix.com/console/account/advanced/api-key
# Sent as the `x-api-key` header. 200 requests/minute.
#
# Unlike the Mailchimp key above, this one IS needed in production: the
# ticket-status route reads it to show "Sold out" / "Registration closed" on
# upcoming event pages. Set it on Vercel too.
#
# The split that makes that safe: /v1/events, /check-in-count and /tags carry
# no personal data and may be called from app/. /orders and /tickets carry
# names, emails, mobiles, street addresses and LIVE ACCESS CODES — they are
# scripts-only, write to /private/ or tmp/, and never to lib/data/json/.
# lib/humanitix/client.ts deliberately does not implement them, so app/ cannot
# import them by mistake.
# ============================================================
HUMANITIX_API_KEY=

# ============================================================
# Cron / scheduled jobs
# ============================================================
# Bearer token protecting cron + admin trigger endpoints (newsletter draft,
# approve, etc.). MUST match the CRON_SECRET configured in Vercel.
CRON_SECRET=your_cron_secret

# Stack Auth: removed 2026-09-06. Nothing under app/, lib/ or components/ ever
# imported it, and NEXT_PUBLIC_STACK_PROJECT_ID,
# NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY and STACK_SECRET_SERVER_KEY have been
# deleted from Vercel. Do not re-add them.

# ============================================================
# Script-only settings (never set on Vercel)
# ============================================================
# scripts/reset-db-and-create-admin.ts refuses to seed an admin without an
# explicit password rather than baking in a known one.
ADMIN_SEED_EMAIL=
ADMIN_SEED_PASSWORD=

# Path to a sibling NZ-SheSharp/she-sharp-reports checkout, read by
# scripts/internal-report/build-record.ts. Leave empty if you do not have one:
# the script then declines instead of writing somewhere unexpected.
SHESHARP_REPORTS_DIR=

# Raw Humanitix export vault — the sibling of MAILCHIMP_VAULT_DIR below, and the
# same rule: it holds real names, addresses, opt-in IPs and live access codes,
# and its contents never enter lib/data/json/ (CI has a leak guard for exactly
# that). Leave it empty unless you hold the archive checkout.
HUMANITIX_VAULT_DIR=

# How long the sync-event-from-slack skill caches the Slack user directory, in
# minutes. Optional.
SLACK_USERS_CACHE_TTL_MIN=

# Explicit path to a globally installed Playwright, for the two verification
# scripts that drive a browser. When set, nothing else is tried.
PLAYWRIGHT_MODULE_PATH=

# Output directory for scripts/optimize-images.mts. Worth setting: the script's
# default input path is absolute and points at one developer's machine.
CURATED_OUT=

# Not in this file, deliberately: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID,
# VERCEL_CLI_VERSION and GH_TOKEN. Those are GitHub Actions inputs, not dotenv
# ones — the first is a repository secret and the rest are literals in
# .github/workflows/deploy.yml. An audit that reconciles this file against
# `process.env` alone will report them missing; they are not.
# docs/deployment/CREDENTIAL_INVENTORY.md holds them.

# ============================================================
# Stripe Payments (https://dashboard.stripe.com)
# ============================================================
# Mode switch: "test" or "live" (controls which key set to use).
# UNSET MEANS LIVE. lib/stripe/config.ts defaults to 'live', so an empty value
# on a new machine reaches the real card processor.
STRIPE_MODE=test

# The eight prefixed names below are invisible to a plain grep for process.env:
# getStripeEnv() in lib/stripe/config.ts builds them at runtime with
# name.replace('STRIPE_', 'STRIPE_TEST_' | 'STRIPE_LIVE_'), falling back to the
# un-prefixed form. Any audit of "which variables still have a reader" has to
# special-case them, or it will report six live Stripe variables as deletable.
# Stripe has no CLI or API for key management either: listing and rotating keys
# are console-only.

# Test Mode Keys (https://dashboard.stripe.com/test/apikeys)
STRIPE_TEST_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_TEST_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_TEST_WEBHOOK_SECRET=whsec_your_test_webhook_secret
STRIPE_TEST_ANNUAL_PRICE_ID=price_your_test_annual_price_id

# Live Mode Keys (https://dashboard.stripe.com/apikeys)
STRIPE_LIVE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_LIVE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key
STRIPE_LIVE_WEBHOOK_SECRET=whsec_your_live_webhook_secret
STRIPE_LIVE_ANNUAL_PRICE_ID=price_your_live_annual_price_id
